// MUI Components
import { TableRow, styled } from '@mui/material';

const TableRowBase = styled(TableRow)(({ theme }) => ({
  backgroundColor: theme.palette.common.white,
}));

export default TableRowBase;
